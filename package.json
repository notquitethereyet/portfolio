{
  "name": "scraper-astro",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "pnpm build && git add . && git commit -m 'Deploy to custom domain' && git push"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.3",
    "astro": "^5.6.1",
    "tailwindcss": "^4.1.3"
  },
  "devDependencies": {
    "@types/node": "^22.14.0"
  }
}
